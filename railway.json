{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm run reset"
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 200,
    "restartPolicyType": "never"
  }
}
