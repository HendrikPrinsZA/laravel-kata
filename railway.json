{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm run reset"
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "never"
  }
}
