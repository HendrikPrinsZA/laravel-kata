{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "php artisan migrate --seed"
  },
  "deploy": {
    "startCommand": "php artisan octane:start --server=roadrunner --host=0.0.0.0 --rpc-port=6001 --port=80",
    "restartPolicyType": "never"
  }
}
