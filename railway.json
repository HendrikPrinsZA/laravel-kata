{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "php artisan migrate --seed"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=80",
    "restartPolicyType": "never"
  }
}
